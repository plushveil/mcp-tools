import * as fs from 'fs'
import * as path from 'path'

// notify('notifications/prompts/list_changed')

/**
 *
 */
export async function onToolListChanged (tools: string[]) : Promise<void> {

}

/**
 *
 */
function parsePromptFile (file: string): void {

}
